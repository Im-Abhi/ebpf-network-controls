# Build artifacts
/bin/
/firewallctl

# Compiled eBPF objects (generated by `make generate` on Linux)
*.o

# Benchmark run output (raw traffic logs + summary TSVs). Force-add
# (git add -f) a finalised run you want committed.
/benchmark/results/

# OS junk
Thumbs.db
.DS_Store